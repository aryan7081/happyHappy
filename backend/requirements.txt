# Django
Django==5.2.3
djangorestframework==3.16.0
django-cors-headers==4.7.0
django-environ==0.12.0

# Database (use binary for deployment - no libpq build required)
psycopg2-binary==2.9.10

# Media / images
Pillow==11.2.1

# Payments
razorpay==1.4.2

# Production WSGI server
gunicorn==23.0.0

# Static files in production (optional; add to MIDDLEWARE if using)
whitenoise==6.8.2
